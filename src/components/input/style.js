import { styled, css } from "styled-components";

export const Form = styled.form`
	display: flex;
	align-items: center;
	gap: 10px;

	input {
		width: 200px;
		height: 40px;
		padding: 0 12px 0;
		outline: none;
		border-radius: 8px;
	}
`;
